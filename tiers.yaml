# Helm deployment tiers — tier N completes before tier N+1 begins.
# Each tier is a list of helm chart names to deploy in parallel.
# Referenced by helm-upgrade and helm-rollback via helm-common.sh.
#
# Copy this file to your project root and customize for your cluster.
# helm-common.sh searches: $INFRA_ROOT/tiers.yaml → k8-lib/tiers.yaml

tiers:
  - name: "Storage & Caching"
    charts:
      # - redis
      # - valkey
      # - timescaledb
      # - karpenter
      # - keda
      # - openebs-lvm

  - name: "Secrets Management"
    charts:
      # - infisical

  - name: "Observability & Policy"
    charts:
      # - infisical-operator
      # - signoz
      # - vpa
      # - goldilocks
      # - kubecost
      # - parca

  - name: "Ingress & Load Balancing"
    charts:
      # - nginx-ingress
      # - aws-lb-controller

  - name: "Databases"
    charts:
      # - mysqldb
      # - timescaledb

  - name: "Database Proxies & Caching"
    charts:
      # - pgbouncer
      # - proxysql
      # - redis
      # - valkey

  - name: "Background Workers"
    charts:
      # - celery

  - name: "Applications"
    charts:
      # - backend
      # - frontend
      # - api
      # - wordpress

  - name: "Admin & Tooling"
    charts:
      # - admin
